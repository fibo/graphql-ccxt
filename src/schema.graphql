scalar Symbol

type Ticker {
  symbol: String!
  last: Float
}

type Client {
  key: String!
  exchange: String!
  ticker(symbol: Symbol!): Ticker
  tickers(symbols: [Symbol]): [Ticker]
}

type Query {
  client(key: String!): Client

  clients: [Client]!
}

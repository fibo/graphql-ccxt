mutation {
  createOrder(
    client: { exchange: "binance", label: "My Binance" }
    order: { type: market, side: buy, symbol: "BTC/BUSD", amount: 0.28 }
  ) {
    datetime
    status
    price
  }
}

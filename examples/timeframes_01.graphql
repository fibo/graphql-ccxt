{
  client(exchange: "binance") {
    exchange
    timeframes
  }
  bitfinex: client(exchange: "bitfinex") {
    timeframes
  }
  bittrex: client(exchange: "bittrex") {
    timeframes
  }
  kraken: client(exchange: "kraken") {
    timeframes
  }
}

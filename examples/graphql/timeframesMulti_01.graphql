{
  timeframesMulti(
    list: [
      { client: { exchange: "binance" } }
      { client: { exchange: "bittrex" } }
      { client: { exchange: "bitfinex" } }
    ]
  ) {
    client {
      exchange
    }
    timeframes
  }
}

{
  tickersMulti(
    list: [
      { client: { exchange: "binance" }, symbols: ["BTC/BUSD", "ETH/BUSD"] }
      { client: { exchange: "kraken" }, symbols: ["BTC/USDT"] }
    ]
  ) {
    client {
      exchange
    }
    tickers {
      symbol
      last
    }
  }
}
